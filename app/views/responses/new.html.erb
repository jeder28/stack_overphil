<%= form_for @response, url: {action: "create"} do |f| %>
  <div>
    <%= f.hidden_field :question_id, :value => @question.id %>
    <%= f.label :content, 'Make a Comment:'%><br>
    <%= f.text_area :content, size: "24x6" %>
  </div>
  <div>
    <%= f.submit 'comment' %>
  </div>
<% end %>

